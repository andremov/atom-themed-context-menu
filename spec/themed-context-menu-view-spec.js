'use babel';

import ThemedContextMenuView from '../lib/themed-context-menu-view';

describe('ThemedContextMenuView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
